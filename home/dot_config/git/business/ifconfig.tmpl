{{- $data := include "data.toml" | fromToml -}}
{{- with $data.data }}
[user]
    name = "{{ .business_name }}"
    email = {{ .business_githubUser }}@users.noreply.github.com
    signingkey = ~/.ssh/github_business_signing_ed25519.pub
{{- end }}